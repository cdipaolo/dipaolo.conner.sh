box: ubuntu:latest
build:
    steps:
        - arjen/hugo-build:
            version: "0.14"
            theme: gindoro
        - install-packages:
            packages: git
deploy:
    steps:
        - lukevivier/gh-pages@0.2.1:
            token: $GIT_TOKEN
            domain: dipaolo.conner.sh
            basedir: public
