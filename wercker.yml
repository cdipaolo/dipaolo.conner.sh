box: centos@latest
build:
    steps:
        - arjen/hugo-build:
            version: "0.14"
            theme: gindoro
deploy:
    - lukeviver/gh-pages:
        token: $GIT_TOKEN
        domain: dipaolo.conner.sh
        basedir: public
        repo: cdipaolo/cdipaolo.github.io
